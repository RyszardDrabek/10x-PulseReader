name: RSS Feed Fetcher

on:
  workflow_dispatch: # Allow manual trigger
  schedule:
    # Run every 15 minutes
    - cron: '*/15 * * * *'

jobs:
  fetch-rss:
    name: Fetch RSS Feeds
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Fetch All RSS Feeds (with retries)
        env:
          DEPLOYMENT_URL: ${{ secrets.APP_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          RETRY_DELAY_MS: ${{ vars.RETRY_DELAY_MS || '2000' }}
          MAX_RETRIES: ${{ vars.MAX_RETRIES || '50' }}
        run: |
          if [ -z "$DEPLOYMENT_URL" ]; then
            echo "❌ Error: APP_URL secret is not set"
            exit 1
          fi

          if [ -z "$SUPABASE_SERVICE_ROLE_KEY" ]; then
            echo "❌ Error: SUPABASE_SERVICE_ROLE_KEY secret is not set"
            exit 1
          fi

          # Set defaults if not provided (GitHub Actions vars might not be set)
          RETRY_DELAY_MS=${RETRY_DELAY_MS:-2000}
          MAX_RETRIES=${MAX_RETRIES:-50}

          echo "🚀 RSS Fetch All - Processing All Sources"
          echo "============================================================"
          echo "📍 Endpoint: $DEPLOYMENT_URL/api/cron/fetch-rss"
          echo "⏱️  Retry Delay: ${RETRY_DELAY_MS}ms"
          echo "🔄 Max Retries: ${MAX_RETRIES}"
          echo "============================================================"

          npm run fetch:all-rss
